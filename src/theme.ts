import { extendTheme, theme } from "@chakra-ui/react";

export default extendTheme({
	colors: {
        primary: "#00fff7",
		secondary: "#8bacda",
		bgLight: "#14253d",
		bgDark: "#0d192b",
	},
	fonts: {
		body: "Outfit, sans-serif",
	}
});
